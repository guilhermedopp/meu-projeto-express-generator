<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
      margin-top: 1rem;
    }
    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      font-size: 0.95rem;
      vertical-align: top;
    }
    th {
      background: #f4f4f4;
      text-align: left;
    }
    tbody tr:nth-child(even) {
      background: #fafafa;
    }
    .actions-container {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .actions-container form {
      margin: 0;
      padding: 0;
      border: none;
      background: none;
      box-shadow: none;
    }
    
    .btn {
      display: inline-block;
      padding: 8px 12px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
      font-size: 0.9em;
      font-weight: bold;
      color: white;
      
      width: 85px;       
      text-align: center;
      box-sizing: border-box;
    }
    
    .btn-edit {
      background-color: #007bff;
    }
    .btn-edit:hover {
      background-color: #0056b3;
    }
    .btn-delete {
      background-color: #dc3545;
    }
    .btn-delete:hover {
      background-color: #c82333;
    }
  </style>

</head>
<body>
  <h1><%= title %></h1>
  <p><a href="/contato">Novo contato</a> | <a href="/">Home</a></p>

  <% if (!contatos || contatos.length === 0) { %>
    <p>Nenhum contato cadastrado ainda.</p>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Nome</th>
          <th>E-mail</th>
          <th>Idade</th>
          <th>Gênero</th>
          <th>Interesses</th>
          <th>Mensagem</th>
          <th>Criado em</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% contatos.forEach(c => { %>
          <tr>
            <td><%= c.id %></td>
            <td><%= c.nome %></td>
            <td><%= c.email %></td>
            <td><%= c.idade || '-' %></td>
            <td><%= c.genero || '-' %></td>
            <td>
              <% if (c.interesses) { %>
                <%= c.interesses.split(',').join(', ') %>
              <% } else { %>
                -
              <% } %>
            </td>
            <td><%= c.mensagem %></td>
            <td><%= c.criado_em %></td>
            <td class="actions">
              <div class="actions-container">
                <a href="/contato/<%= c.id %>/edit" class="btn btn-edit">Editar</a>
                <form action="/contato/<%= c.id %>/delete" method="post"
                      onsubmit="return confirm('Tem certeza que deseja excluir o contato <%= c.nome %>?');">
                  <button type="submit" class="btn btn-delete">Excluir</button>
                </form>
              </div>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>